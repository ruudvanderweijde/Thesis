\usepackage{fullpage}% use the full page

% inline referencing
\usepackage{nameref} % refer to sections by name
\usepackage{hyperref} % Any links in resulting glossary will not be "clickable" unless you load the glossaries package after the hyperref package.

% referencing
\usepackage[backend=biber,style=alphabetic]{biblatex}% biblatex stuff
\addbibresource{bib/Thesis.bib}% set the bib library file 

% include sub/pdf files
\usepackage[final]{pdfpages}% needed to include pdf files
\usepackage{subfiles}% able to include files in subfolder

% table packages
\usepackage{tabularx} % package for tables, required for generated php-analysis tables
\usepackage{booktabs} % To thicken table lines
\usepackage{numprint} % print numbers in table

% glossary
\usepackage[nonumberlist,toc]{glossaries} % used for glossary

% used to display images
\usepackage{graphicx}
\usepackage{wrapfig}
\usepackage{caption}
\usepackage{subcaption}
% create a tree
\usepackage{forest}

% display random text
\usepackage{blindtext}

% advanced maths
%\usepackage{mathtools}
\usepackage{amsmath}

% be able to display 'plain' quotes (')
\usepackage[T1]{fontenc}  % access \textquotedbl
\usepackage{textcomp}     % access \textquotesingle

% strikethrough
\usepackage[normalem]{ulem} % use for strikethrough \sout{text}

% this line prevents the error: "too many unprocessed floats"
% link: http://www.douglasvanbossuyt.com/2008/11/18/latex-too-many-unprocessed-floats-problem-and-solution/
\usepackage[section]{placeins}

% algorithms
\usepackage[ruled,linesnumbered]{algorithm2e} % used to display algorithms

% numbered linguistics 
% more info: https://en.wikibooks.org/wiki/LaTeX/Linguistics
%\usepackage{gb4e} 

% helper for double brackets and parentheses
% http://ctan.org/pkg/stmaryrd
\usepackage{stmaryrd}

% helper for charts
\usepackage{tikz,tikz-3dplot}
\usepackage{pgfplots}

% align prooftrees on one line
\newenvironment{bprooftree}
  {\leavevmode\hbox\bgroup}
  {\DisplayProof\egroup}
	
% algorithm helper
\makeatletter
\newcommand{\algorithmfootnote}[2][\footnotesize]{%
  \let\old@algocf@finish\@algocf@finish% Store algorithm finish macro
  \def\@algocf@finish{\old@algocf@finish% Update finish macro to insert "footnote"
    \leavevmode\rlap{\begin{minipage}{\linewidth}
    #1#2
    \end{minipage}}%
  }%
}
\makeatother

%proof tree layout
%\fact{name}{title}{description}
\newcommand{\fact}[3]{
    \paragraph{#2} #3 
    \input{tex/prooftree/#1}
    Code sample: \\
    \lstinputlisting[language=PHP,label=#1,caption=#2]{src/php/#1.php}
    \hrulefill
    \\
}
