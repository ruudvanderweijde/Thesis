self   // in class C -> [self]   = classType(C)
parent // in class C -> [parent] = parentOf(classType(C))
static // in class C -> [static] = classType(C) or <: classType(C)
$this  // in class C -> [$this]  = classType(C) or <: classType(C)
